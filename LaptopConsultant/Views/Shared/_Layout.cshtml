<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    @* <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous"> *@
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.6.0/remixicon.css" rel="stylesheet" />
    
    <style>
        a {
            text-decoration: none !important;
        }
        body {
            font-family: 'Inter', sans-serif;
        }

        .nav-link {
            transition: color 0.3s ease, transform 0.2s ease;
        }

            .nav-link:hover {
                color: #2563eb;
                transform: translateY(-1px);
            }

        .dropdown-menu {
            transition: opacity 0.3s ease, transform 0.3s ease;
        }

        .btn-primary {
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

            .btn-primary:hover {
                transform: scale(1.03);
            }
            .Logo{
            width: 100px;
            height: 100px;
            border-radius: 50%;
        }

        :root {
            --header-primary: #667eea;
            --header-primary-dark: #5a67d8;
            --header-white: #fff;
            --header-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            --header-shadow-hover: 0 8px 30px rgba(102, 126, 234, 0.15);
            --transition: all 0.3s ease-in-out;
        }

        .modern-header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: var(--header-shadow);
        }

        .header-nav {
            max-width: 1400px;
            margin: auto;
            padding: 1rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
        }

        .header-logo-container {
            display: flex;
            align-items: center;
            text-decoration: none;
        }

        .header-logo {
            width: 44px;
            height: 44px;
            border-radius: 12px;
            object-fit: cover;
            margin-right: 12px;
            box-shadow: 0 2px 6px rgba(102, 126, 234, 0.2);
        }

        .header-logo-text {
            font-size: 1.4rem;
            font-weight: 700;
            background: linear-gradient(135deg, var(--header-primary), #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .header-nav-links {
            display: flex;
            gap: 1rem;
            align-items: center;
        }

        .header-nav-link {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 10px 16px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 500;
            font-size: 0.95rem;
            color: #4a5568;
            transition: var(--transition);
        }

            .header-nav-link:hover {
                background: linear-gradient(135deg, var(--header-primary), #764ba2);
                color: white;
                box-shadow: var(--header-shadow-hover);
            }

        .header-search-container {
            display: flex;
            align-items: center;
            margin-top: 1rem;
        }

        .header-search-form {
            display: flex;
            background: #fff;
            border-radius: 50px;
            overflow: hidden;
            border: 2px solid rgba(102, 126, 234, 0.1);
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
        }

        .header-search-input {
            padding: 12px 18px;
            font-size: 0.95rem;
            border: none;
            outline: none;
            width: 200px;
        }

        .header-search-btn {
            background: linear-gradient(135deg, var(--header-primary), #764ba2);
            border: none;
            color: white;
            font-weight: 600;
            font-size: 0.9rem;
            padding: 12px 18px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .header-mobile-menu-btn {
            display: none;
            flex-direction: column;
            gap: 5px;
            background: none;
            border: none;
            cursor: pointer;
        }

            .header-mobile-menu-btn span {
                width: 24px;
                height: 3px;
                background: var(--header-primary);
                border-radius: 2px;
            }

        /* Mobile Responsive */
            .footer-center{
                display: flex;
                justify-content: space-between;
                align-items: center;
                padding: 0 6rem;
            }
    </style>
</head>
<body class="bg-gray-50">
    <header class="modern-header">
        <nav class="header-nav">
            <!-- Logo -->
            <a href="/" class="header-logo-container">
                <img src="~/images/logo.jpg" alt="Logo" class="header-logo" />
                <span class="header-logo-text">TechConsult</span>
            </a>

            <!-- Mobile Menu Button -->
            <button class="header-mobile-menu-btn" aria-label="Toggle menu">
                <span></span>
                <span></span>
                <span></span>
            </button>

            <!-- Navigation Links -->
            <div class="header-nav-links">
                <a href="@Url.Action("Index", "Home")" class="header-nav-link">
                    <i class="fas fa-home"></i><span>Trang chủ</span>
                </a>
                <a href="@Url.Action("Index", "Laptop")" class="header-nav-link">
                    <i class="fas fa-laptop-code"></i><span>Tư vấn</span>
                </a>
                <a href="@Url.Action("Compare", "Laptop")" class="header-nav-link">
                    <i class="fas fa-balance-scale"></i><span>So sánh</span>
                </a>
                <a href="@Url.Action("Index", "Statistics")" class="header-nav-link">
                    <i class="fas fa-chart-line"></i><span>Thống kê</span>
                </a>
            </div>

            <!-- Search Form -->
            <div class="header-search-container">
                <form asp-controller="Laptop" asp-action="Search" class="header-search-form" method="get">
                    <input type="text" name="query" class="header-search-input" placeholder="Tìm kiếm laptop..." />
                    <button type="submit" class="header-search-btn">
                        <i class="fas fa-search"></i><span>Tìm</span>
                    </button>
                </form>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main>
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white py-8">
        <div class="container mx-auto px-4 grid grid-cols-1 md:grid-cols-4 gap-6">
            <div>
                <h3 class="text-lg font-bold mb-3">Logo</h3>
                <p class="text-gray-400 text-sm">Hệ thống tư vấn laptop thông minh giúp bạn tìm chiếc laptop hoàn hảo.</p>
            </div>
            <div>
                <h3 class="text-lg font-bold mb-3">Liên kết nhanh</h3>
                <ul class="space-y-1">
                    <li><a href="@Url.Action("Index", "Home")" class="text-gray-400 hover:text-primary nav-link text-sm">Trang chủ</a></li>
                    <li><a href="@Url.Action("Index", "Laptop")" class="text-gray-400 hover:text-primary nav-link text-sm">Tư vấn laptop</a></li>
                    <li><a href="@Url.Action("Compare", "Laptop")" class="text-gray-400 hover:text-primary nav-link text-sm">So sánh laptop</a></li>
                    <li><a href="@Url.Action("Index", "Statistics")" class="text-gray-400 hover:text-primary nav-link text-sm">Thống kê xu hướng</a></li>
                    <li><a href="#" class="text-gray-400 hover:text-primary nav-link text-sm">Tin tức công nghệ</a></li>
                </ul>
            </div>
            <div>
                <h3 class="text-lg font-bold mb-3">Hỗ trợ</h3>
                <ul class="space-y-1">
                    <li><a href="#" class="text-gray-400 hover:text-primary nav-link text-sm">Hướng dẫn sử dụng</a></li>
                    <li><a href="#" class="text-gray-400 hover:text-primary nav-link text-sm">Câu hỏi thường gặp</a></li>
                    <li><a href="#" class="text-gray-400 hover:text-primary nav-link text-sm">Liên hệ hỗ trợ</a></li>
                    <li><a href="#" class="text-gray-400 hover:text-primary nav-link text-sm">Chính sách bảo mật</a></li>
                    <li><a href="#" class="text-gray-400 hover:text-primary nav-link text-sm">Điều khoản sử dụng</a></li>
                </ul>
            </div>
            <div>
                <h3 class="text-lg font-bold mb-3">Liên hệ</h3>
                <p class="text-gray-400 text-sm mb-2"><i class="ri-map-pin-line mr-1"></i>123 Nguyễn Văn Linh, Quận 7, TP. Hồ Chí Minh</p>
                <p class="text-gray-400 text-sm mb-2"><i class="ri-phone-line mr-1"></i>0123 456 789</p>
                <p class="text-gray-400 text-sm mb-3"><i class="ri-mail-line mr-1"></i>info@tuvanlaptopdep.vn</p>
                <h4 class="text-base font-semibold mb-2">Đăng ký nhận tin</h4>
                <form class="flex">
                    <input type="email" placeholder="Email của bạn" class="border border-gray-600 rounded-l-md px-3 py-1.5 bg-gray-800 text-white text-sm focus:outline-none focus:ring-2 focus:ring-primary">
                    <button type="submit" class="bg-primary text-white px-3 py-1.5 rounded-r-md hover:bg-blue-600 text-sm">Đăng ký</button>
                </form>
            </div>
        </div>
        <div class="footer-center border-t border-gray-700 mt-6 pt-3 text-center d-flex justify-content-between">
            <p class="text-gray-400 text-sm">© 2025 Tư vấn Laptop. Tất cả quyền được bảo lưu.</p>
            <div class="flex justify-center space-x-3 mt-2">
                <img src="/images/visa.webp" alt="Visa" class="h-5">
                <img src="/images/Mastercard.png" alt="Mastercard" class="h-5">
                <img src="/images/paypal.webp" alt="Paypal" class="h-5">
                <img src="/images/momo.webp" alt="Momo" class="h-5">
            </div>
        </div>
    </footer>

    @RenderSection("Scripts", required: false)
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const menuBtn = document.querySelector(".header-mobile-menu-btn");
            const navLinks = document.querySelector(".header-nav-links");

            menuBtn.addEventListener("click", () => {
                navLinks.classList.toggle("active");
            });
        });
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q" crossorigin="anonymous"></script>
</body>
</html>